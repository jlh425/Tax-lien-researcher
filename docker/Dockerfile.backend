# ---------- Base stage ----------
FROM python:3.12-slim AS base

# System dependencies for psycopg, Tesseract OCR, Poppler (PDF), OpenCV
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        build-essential \
        libpq-dev \
        tesseract-ocr \
        poppler-utils \
        libgl1 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# ---------- Dependencies ----------
COPY pyproject.toml ./
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir .

# ---------- Application ----------
COPY src/ ./src/

EXPOSE 8000

# Default production command (overridden with --reload in dev compose)
CMD ["uvicorn", "aloha.main:app", "--host", "0.0.0.0", "--port", "8000"]
